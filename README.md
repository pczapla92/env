# About

Dev env rc files setup

# Usage

```bash
python3 install.py bash       # source bash config into ~/.bashrc
python3 install.py git        # apply git aliases globally
python3 install.py vim        # symlink vimrc to ~/.vimrc
python3 install.py all        # apply all of the above
```
